# Change: example

in df.py def s,

I want to add one more argument after r, before f, called a. It will be a list of items accepted in with_columns, at least it needs pl.DataFrame or expr. Then s will by default display everything in a.

Add as little code as possible
