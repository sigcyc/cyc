Goal: extract the path component to another location

Context: 
    df_types.yaml data field contains a path that's where to load the data. I have a default data_dir, I'd like to add an option data: path: default to load from that default direction

Requirement:
    The project should be structured in a nice way that's intuitive where to change data_dir
