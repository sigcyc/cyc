Goal: write a simple template script to save toy data. 

Context: examples/save_toy_data.py


Requirements: 
* Use Typer package
* The data will be saved at df_types.yaml/data_dir/toy_data
* add an option of date
* add an option of data_dir that might override default location
* add an option that will actually write file or not
* write minimum amount of code

Verification: run the script on a random data, and it should save a dataset at the correct place
